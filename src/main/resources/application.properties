spring.application.name=entregas

# Configurações do banco de dados PostgreSQL
spring.datasource.url=jdbc:postgresql://147.79.107.77:5432/entregas
spring.datasource.username=dba
spring.datasource.password=Dba12271000

# Configurações JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Logs de SQL no console (opcional, para depuração)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# swagger-ui rota costumizada
springdoc.swagger-ui.path=/docs
